import styled from 'styled-components';

import { variables } from '../../styles/variables';

export const SLayout = styled.div`
  display: flex;
`;

export const Content = styled.div`
  padding: calc(${variables.xxxlSpacing} * 2);
`;
